# env.conf — per-host configuration for shell and editor dotfiles
# Copy this file to ~/.config/env.conf and adjust values for your environment.
# Lines starting with # and blank lines are ignored.

# Launch tmux automatically on interactive shell login
# Set to false on hosts where tmux is managed externally (e.g. devai container)
AUTOSTART_TMUX=true

# Tmux session name to attach or create
TMUX_SESSION_NAME=main

# Run keychain for SSH key management (useful on long-lived hosts)
SETUP_KEYCHAIN=false

# Space-separated list of SSH keys for keychain
KEYCHAIN_KEYS=~/.ssh/id_rsa_github

# Colon-separated extra directories to prepend to PATH
EXTRA_PATH=

# Check for Neovim in /opt on shell start and offer to install if missing
INSTALL_NVIM=true

# Enable gemini-cli.nvim plugin in Neovim
GEMINI_NVIM=false
